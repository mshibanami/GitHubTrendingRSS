name: Fetch and Generate RSS Feeds

on:
  schedule:
    - cron:  '0 1 * * *'
jobs:
  build:

    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: ./build.sh
    - name: Deploy
      run: ./deploy.sh
